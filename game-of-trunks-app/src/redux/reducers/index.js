const INITIAL_STATE = {
  isFetching: false,
  characterData: '',
  errorMessage: '',
}

const reducer = (state=INITIAL_STATE, action) => {
  switch (action.type) {
    // case value:
      
    //   break;
  
    default:
      return state;
  }
}

export default reducer;